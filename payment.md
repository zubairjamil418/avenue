# Stripe Payment Keys

## Secret Key (Server-side only — `apps/api/.env`)
```
STRIPE_SECRET_KEY=sk_test_...  (see apps/api/.env)
```

## Publishable Key (Client-side — `apps/web/.env`)
```
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...  (see apps/web/.env)
```

## Webhook Secret (Server-side only — `apps/api/.env`)
```
STRIPE_WEBHOOK_SECRET=whsec_...  (see apps/api/.env)
```
