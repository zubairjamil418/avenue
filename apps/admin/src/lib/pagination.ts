/**
 * Centralized pagination config for the admin dashboard.
 * Change DEFAULT_PER_PAGE here to update all pages at once.
 */
export const DEFAULT_PER_PAGE = 25;

/** Commonly used per-page size options shown in dropdowns */
export const PER_PAGE_OPTIONS = [10, 25, 50, 100] as const;
